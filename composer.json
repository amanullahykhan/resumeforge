{
    "name": "resumeforge/pro",
    "description": "AI-assisted resume builder: wizard + visual builder + server-side PDF/DOCX/PNG/HTML exports. Shared-host friendly.",
    "type": "project",
    "license": "MIT",
    "require": {
        "php": ">=7.4",
        "mpdf/mpdf": "^8.2",
        "phpoffice/phpword": "^1.3"
    },
    "suggest": {
        "ext-imagick": "Enables PNG export (page 1 of the generated PDF)",
        "ext-gd": "Photo upload resizing (required by api/upload_photo.php)",
        "ext-pdo_sqlite": "Persistent draft storage (auto-falls back to JSON files without it)"
    },
    "config": {
        "optimize-autoloader": true,
        "policy": {
            "advisories": {
                "block": false
            }
        }
    }
}